SECRET_KEY=change-me-in-production
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

DB_ENGINE=django.db.backends.postgresql
DB_NAME=tyva_transit
DB_USER=tyva
DB_PASSWORD=tyva
DB_HOST=127.0.0.1
DB_PORT=5432

# === ПОЧТА (обязательно для отправки билетов) ===
# Сейчас в .env стоят заглушки — письма НЕ уходят, пока не укажете реальные данные!

EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend

# --- Gmail ---
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
EMAIL_HOST_USER=ваш@gmail.com
EMAIL_HOST_PASSWORD=пароль-приложения-16-символов
DEFAULT_FROM_EMAIL=TyvaTransit <ваш@gmail.com>

# --- Mail.ru (раскомментируйте вместо Gmail) ---
# EMAIL_HOST=smtp.mail.ru
# EMAIL_PORT=465
# EMAIL_USE_TLS=False
# EMAIL_USE_SSL=True
# EMAIL_HOST_USER=ваш@mail.ru
# EMAIL_HOST_PASSWORD=пароль-приложения
# DEFAULT_FROM_EMAIL=TyvaTransit <ваш@mail.ru>

# Проверка: python manage.py test_email ваш@email.ru

CARRIER_NOTIFY_EMAIL=
